{
  "name": "create-react-app-fcclock",
  "version": "0.5.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.0",
    "bootstrap": "^4.5.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:counter": "netlify-lambda serve src/counter",
    "build": "run-p build:**",
    "build:app": "react-scripts build",
    "build:counter": "netlify-lambda build src/counter",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "netlify-lambda": "^2.0.1",
    "npm-run-all": "^4.1.5"
  }
}
